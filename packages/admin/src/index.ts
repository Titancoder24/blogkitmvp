/**
 * `@blogkit/admin` — admin React app.
 *
 * Implementation arrives in week 4 of §12 (admin foundation), wires up the
 * resume-builder-style editor in week 6 (live preview pane), and the live
 * scoring panel in week 10. This file exists as the workspace anchor.
 *
 * Surfaces (PRD §5, §5A, §5B, §5C, §5D):
 *   - /admin/login                      Supabase Auth (email/password)
 *   - /admin                            post list with filters
 *   - /admin/posts/new                  resume-builder editor, blank
 *   - /admin/posts/[id]                 resume-builder editor for an existing post
 *   - /admin/media                      media library
 *   - /admin/visibility                 site-wide AI Visibility dashboard
 *   - /admin/templates                  visual custom-template builder
 *   - /admin/settings                   site / org / theme settings
 *   - /admin/tokens                     MCP token issuance
 */
export {};
