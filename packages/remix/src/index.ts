/**
 * `@blogkit/remix` — Remix v2+ adapter.
 *
 * Mirrors the Next.js adapter's surface (`installBlogKit`, public route
 * components, feed/sitemap/llms.txt loaders, MCP HTTP route). Wired up in
 * week 2 of §12 once the Next.js adapter is shipped.
 */
export {};
